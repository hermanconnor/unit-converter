<div class='text-center'>
  <h1 class='text-4xl font-bold text-red-600'>Something went wrong</h1>
  <p class='mt-4 text-xl font-medium'>{{message}}</p>

  {{#if error.stack}}
    <pre
      class='mt-4 p-4 bg-gray-100 text-left text-sm rounded shadow overflow-x-auto'
    >
      {{error.stack}}
    </pre>
  {{/if}}

  <a href='/' class='mt-6 inline-block text-blue-600 hover:underline'>Return to
    homepage</a>
</div>